{{ define "main" }}


    <nav>
    {{ range $link, $title := GetChildPages "hobbies" true -}}
        <a href="{{ $link }}"> {{ $title }} </a>
    {{ end }}
    </nav>
    <h1>{{ .Title }}</h1>

    <p>Status: {{ .Status }}</p>

    <p>Roles:
        <ul>
            {{ range .Roles }}
                <li>{{ . }}</li>
            {{ end }}
        </ul>
    </p>

    {{ .Content }}

{{ end }}